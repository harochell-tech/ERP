# E-B03-2: HTTPS only. Port 80 stays closed, so Let's Encrypt validates with TLS-ALPN on 443.
{$STAGING_HOST} {
	encode zstd gzip
	header Strict-Transport-Security "max-age=31536000"
	reverse_proxy api:8080
}
